<%- include('./_styles') %>
<div class="admin">
    <%- include('./_nav') %>
    <div class="admin__main">

        <div class="adm-hero">
            <div class="adm-crumbs">
                <a href="/admin">Admin</a>
                <span class="sep">/</span>
                <strong>Customers</strong>
            </div>
            <div class="adm-hero__row">
                <div>
                    <h1>Customers</h1>
                    <div class="adm-hero__sub"><%= users.length %> customer<%= users.length === 1 ? '' : 's' %> total</div>
                </div>
            </div>
        </div>

        <form method="get" action="/admin/users" class="admin__search">
            <input type="text" name="q" value="<%= q || '' %>" placeholder="Search by name, email or phone…">
            <button class="btn btn--sm">Search</button>
            <% if (q) { %><a href="/admin/users" class="btn btn--sm btn--ghost">Clear</a><% } %>
        </form>

        <% if (!users.length) { %>
            <div class="adm-empty">
                <div class="adm-empty__art">
                    <svg width="44" height="44" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
                </div>
                <p>No customers match your search.</p>
            </div>
        <% } else { %>
            <div class="adm-table-wrap">
            <div class="adm-table-scroll">
            <table class="data-table">
                <thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Role</th><th class="adm-num">Orders</th><th>Joined</th><th></th></tr></thead>
                <tbody>
                <% users.forEach(function(u) { %>
                    <tr>
                        <td><a href="/admin/users/<%= u.id %>"><strong><%= u.name || '—' %></strong></a></td>
                        <td style="color:var(--adm-ink-2)"><%= u.email %></td>
                        <td style="color:var(--adm-ink-2);font-variant-numeric:tabular-nums"><%= u.phone || '—' %></td>
                        <td><span class="badge badge--<%= u.role.toLowerCase() %>"><%= u.role %></span></td>
                        <td class="adm-num"><%= u._count.orders %></td>
                        <td style="color:var(--adm-ink-3);font-variant-numeric:tabular-nums;font-size:0.84rem"><%= u.createdAt.toISOString().slice(0,10) %></td>
                        <td>
                            <div style="display:flex;gap:6px;justify-content:flex-end">
                                <a href="/admin/users/<%= u.id %>" class="btn btn--sm">Open</a>
                                <% if (u.id !== currentUser.id) { %>
                                    <form action="/admin/users/<%= u.id %>/toggle-role" method="post" onsubmit="return confirm('Toggle role for <%= u.email %>?')" style="display:inline">
                                        <button class="btn btn--sm btn--ghost">Make <%= u.role === 'ADMIN' ? 'User' : 'Admin' %></button>
                                    </form>
                                <% } %>
                            </div>
                        </td>
                    </tr>
                <% }); %>
                </tbody>
            </table>
            </div>
            </div>
        <% } %>
    </div>
</div>
